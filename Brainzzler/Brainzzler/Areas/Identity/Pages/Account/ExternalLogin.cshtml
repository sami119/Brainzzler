@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Регистрация";
}

<p class="text-info">
    Успешно влязохте със <strong>@Model.LoginProvider</strong>.
    Моля въведете имейл адрес за този сайт и натиснете бутона за регистрация за да приключите.
    влизането си.
</p>

<div class="text-center">
    <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
        <div asp-validation-summary="All" class="text-danger"></div>
        <div class="form-group">
            <input asp-for="Input.Email" class="forms-2" placeholder="Имейл" />
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>
        <button type="submit" class="StartGame">Регистрация</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
